from ollama import Ollama

# Initialize client
client = Ollama()

# Define a prompt. This should be invoked in the main screen of the program, and should allow user queries to be entered
prompt = """This is a test prompt, please respond with hello and tell me about yourself briefly."""